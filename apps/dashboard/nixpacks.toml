[phases.setup]
nixPkgs = ["nodejs_22"]

[phases.install]
cmds = ["npm install --legacy-peer-deps"]

[phases.build]
cmds = ["npm run build"]
cacheDirectories = ["node_modules", ".turbo", "dist"]

[phases.start]
cmd = "npm start"
