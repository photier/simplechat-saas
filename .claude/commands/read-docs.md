Read the CLAUDE.md file in the project root and understand:
- Project structure (intergram containers, stats dashboard, n8n webhooks)
- Deployment procedures for each component
- File structure (which files are used vs. backup copies)
- Container configurations (volume mounts vs. docker cp)
- Critical notes about stats dashboard (api.js contains modal code, public/ directory is served)
- Database schema and data flow
- WebSocket real-time features

After reading, provide a brief summary of the most critical deployment procedures.
